# 大明职官图部署目标

用户指定：本网站后续直接部署到虚拟主机 **dmwc1566**。

## 当前状态

- FTP 用户名：`dmwc1566`。
- FTP 服务器地址：`dmwc1566.gotoftp11.com`，端口 `21`。
- 网站根目录：`/wwwroot`，已通过 FTP 目录和默认欢迎页确认。
- 绑定域名：`dmwc.cc`，用户已确认；公共 DNS 指向本主机。
- 协议：主机明确返回不支持 TLS FTP，使用其普通 FTP 服务。
- 密码由用户单独提供，不记录在本文件或项目中。
- 已部署到 `http://dmwc.cc/`，当前发布 `20260906T183529Z`。手机版主发布更新 3 个文件，搜索任所修正后的最终发布更新 2 个文件，均通过 FTP 上传及逐文件回读。公网核对结果保存于 `work/ftp-deployment-receipt.json`，覆盖 574 个公开文件以及数据库、年度、人物、直接官职对应与访问统计接口。PHP 为执行接口，不按源码下载比较；`web.config` 保持 HTTP 禁止下载。
- 浏览器已验证地图、府州层级和官职浮窗。现有 NAS 地址 `http://192.168.31.35:8092/` 保留作旧副本；它不再是本项目后续默认发布目标。
- HTTPS 当前返回自签证书，尚不能称为有效 HTTPS 部署；需在主机面板安装匹配 `dmwc.cc` 的有效证书。当前交付使用 HTTP。

## 上传内容

`npm run build:nas` 输出 `dist-nas/`：React 静态页面、PHP 8.2 历史数据读取和访问统计接口及从 MySQL 提取的版本化读取缓存。线上不依赖 Node.js、NAS 或 Sites；数据主库为 MySQL 8.0，PHP 是必需运行时。

将 `dist-nas/` **内部文件**上传到域名对应的网站根目录，使 `index.html` 直接位于根目录。资源使用 `/assets/`、`/data/` 等根路径，目前不能直接部署到域名的子目录。

只发布构建输出，不上传源码、`.openai/`、`node_modules/`、`work/`、NAS 配置或凭证。站点使用页面内状态导航，不要求服务器运行前端框架。

## 连接信息齐备后的发布

先列出远端网站根目录并检查已有内容，保留可回滚副本后上传站点文件。按主机支持的协议连接；密码只在当前进程或受保护的凭证设施中使用。上传完成后，从网站访问地址独立校验首页、脚本、样式和数据文件，再确认迁移成功。

原 NAS 运维说明保留在 `nas/README.md`，仅供维护现有副本。

## 本机上传工具

`python scripts/deploy-ftp.py` 在隐藏输入提示中读取密码；成功连接后接受 JSON 行命令。先以 `list` 检查根目录，发布用 `{"action":"deploy","path":"/wwwroot"}`，结束用 `{"action":"quit"}`。

该工具先备份即将覆盖的文件，再上传并核验临时文件，最后替换入口页并逐文件下载核对 SHA-256。发布回执位于忽略版本控制的 `work/ftp-deployment-receipt.json`。FTP 核验完成后，仍须从 `http://dmwc.cc/` 检查所有静态文件，才可称网站已迁移成功。

运行 `python scripts/verify-ftp.py` 自动检查公开文件、首页和 HTTPS 证书。验证使用公共 DNS 地址加原始 Host 头，避免本机代理的 DNS 映射干扰；另以浏览器实际访问域名确认页面显示。

## 主机适配

本机为 IIS，默认没有 WebP 和 Markdown MIME 映射，已由 `public/web.config` 补齐；未来构建会一并生成。配置不得移除，否则人物、场景图及部分史料会返回 IIS 404.3。

主机将 PNG/JPG 跳转到 `dmwc-x-cc.img.addlink.cn` 图片加速域名。已经跟随实际跳转并核对文件哈希，内容与发布包一致；该行为无需前端改写路径。

## 年度人物与数据库（2026-09-06）

- 已切换主机 PHP 8.2；PDO MySQL 可用。数据库连接配置位于 `/others/dmwc-history/config.php`，在 `/wwwroot` 之外。线上 DSN 使用 `localhost`；开发通过提供商远程域名连接。配置不在发布包中。
- `atlas_people`、`atlas_tenures`、`atlas_sources`、`atlas_years`、`atlas_eras`、`atlas_offices`、`atlas_datasets` 保存已管理的数据快照。人物任职默认1566，范围1368—1644（不包含南明）。
- 现版本12,269份档案、47,258条仕履/任免/授衔等记载，包含明确标识待原典复核的CBDB资料。不是五品以上逐年全集。已确认身份的档案合并；同名无据不合并。CBDB改编数据的来源与许可见 `/data/history-provenance.html`。
- `scripts/prepare-history.py` 组装可复核导入包；`python scripts/history-db.py --import` 通过隐藏输入连接数据库，先备份旧表，再事务导入并核对所有ID，提交后由 `history_snapshot.py` 读取MySQL实际数据生成版本缓存。不得在独立修改数据库后跳过快照刷新。
- 首次按年读取从PHP版本缓存返回，避免反复连接远程数据库；人物详情分片按需读取，浏览器复用本会话已访问年份和人物。API返回gzip、ETag及5分钟缓存。`action=health`仍实际查询MySQL；`live=1`可在只读核对时绕过读取缓存。
- `public/api/snapshots/manifest.json` 标识数据版本，版本目录先上传、manifest随后、首页最后。旧线上版本目录不删除，保留回滚能力。
- 本地开发：`python scripts/history-dev.py`（隐藏输入密码，PHP监听127.0.0.1:8088），另启动 `npm run dev:nas`（5174，代理/api）。
- 验证：`npx tsc --noEmit`、`python scripts/test_history.py`；设置 `MING_HISTORY_API_URL` 可追加实际HTTP核对；`npm run build:nas` 后通过FTP上传、`python scripts/verify-ftp.py`独立校验公开文件、API及私有配置不可访问。
- 数据回滚使用 `work/history-db-backup-*.json`，文件回滚使用 `work/ftp-backup-*/`。两者必须对应同次发布，私有连接配置保持不变。

本次读取缓存版本：`93313cbf2a6b549e`。线上样本：1566年79位人物/152条年内记录；人物库12,269份档案；687个来源。上一版15次线上样本均返回有效JSON，年度请求0.35—1.70秒，人物详情0.37—0.62秒；gzip下1566年度约12.6KB，人物目录与初始资料约440KB。结果保存于 `work/history-reliability-after.json`。浏览器已核验1416年内阁4人、1580年内阁3人与实际阁衔、1580年兵部尚书及左右侍郎、山东府州辖属图。年表的兼衔、赠衔、未赴任独立标识；不明年份不推定为连续任期。本次未作移动端实机验证。

## CBDB 直接对应（2026-09-06）

- 按用户要求，人物与官职统一直接展示，取消 CBDB 任官提示标签；出处和原始摘要保留在数据库与详情资料中。
- 完整履历替换旧资料点时漏掉的绑定已恢复，并按已知机构和唯一官名补入，共 4,683 条直接官职对应。原有年份、人物身份、任命性质和来源逐条比较未变。
- 修订后数据库导出与导入稿逐条一致；18 项数据检查通过，3 项可选 HTTP 测试未在本地执行，公网独立验证另外覆盖年度、人物及直接绑定。

## 页底访问统计（2026-09-06）

- 页底以一行低调的灰金色小字展示“累计访问 / 今日访问”。自上线起记录真实网页打开次数，不补造上线前数字，不作为独立访客人数。
- 同一页面内切换年份、查看人物及 React 重挂载不重复登记；刷新或重新打开页面形成新的一次访问。后台标签页首次可见时登记。接口失败自动重试一次，使用相同随机请求号避免重复计数。
- MySQL 的 `atlas_visit_meta`、`atlas_visit_days`、`atlas_visit_requests` 保存起始时间、每日总量及去重请求号；不存储 IP、浏览器指纹或用户代理。今日按北京时间计算。历史资料导入不修改这些表。
- `GET /api/visits.php` 只读取；本站页面通过 `POST` 登记。开发域名仅读取，已知爬虫不登记。数据库异常展示“访问统计暂不可用”，不返回虚构数字。
- 首次部署前运行 `python scripts/configure-visits.py`，数据库密码通过隐藏输入提供；建表幂等，保留已有计数。7 项数据库检查使用连接内临时表，不增加正式访问量，涵盖重复请求、跨日与事务回滚。
- 本次 TypeScript、PHP 语法、最终构建及公网逐文件验证通过。公网统计验证只使用 GET，不制造测试访问量。

## 顶部人物搜索（2026-09-06）

- 顶部搜索同时支持人物与官职，沿用 Ctrl K。人物检索基于既有全部 12,269 人目录，不随当前年份缩窄；点击结果打开现有完整履历浮窗。
- 姓名完全匹配优先，兼用既有履历官名、机构和摘要检索。生卒年与摘要辅助区分同名，选择以独立人物 ID 为准，不自动合并档案。不承诺未收录的别名或简繁字转换。
- 首批每类最多 20 项，点击继续显示；索引复用已有数据，逐字检索不新增网络请求。人物目录失败独立显示重试，年度请求失败不阻断人物检索。
- 本地实数检查覆盖张居正、海瑞、朱元璋、同名刘宇、空查询、空结果和既有官职查询。TypeScript 与最终静态构建通过。单次人物检索在本机样本平均约 0.5 毫秒，不代表所有设备耗时。

## 场景姓名牌（2026-09-06）

- 朝堂、皇帝及州县衙署人物牌按“姓名 → 官职 → 品级”显示，随所选年份更新；未载人名显示“本年暂无人名”，不据此推定空缺。原有地面站位不变，长姓名组换行并提供完整文字提示。
- 姓名取年度记录，按人物 ID 去重；包含年内先后与明确任官事件，不把赠衔、未赴任、荣誉头衔或非任职事件标为在职人物。
- 地方标签与官职详情共享任所和官职匹配。明确地方任所但缺少通用官职 ID 的记录，支持地名限定职名，如“淳安知县 / 淳安县”“兴国知县 / 江西兴国县”；不从籍贯或来源地方志推断任所。
- 已核对1566、1573内阁，1566、1580兵部尚书，1562淳安知县海瑞、1563兴国知县海瑞、1376高唐州知州朱守仁；另核对地方隔离、重复来源去重及未赴任/赠衔排除。现数据没有明确吴县年度任所记录，未臆补姓名。
- TypeScript、最终静态构建及差异格式检查通过；保留全部已有历史数据与数据库版本。

## 独立手机体验（2026-09-07）

- 手机布局使用独立页面组件：底部“天下 / 京师 / 人物 / 史料”，紧凑年份栏，按年号与公元年选择的全屏面板。断点为宽度不超过 767px，或粗指针设备宽度不超过 1023px；桌面继续使用原有布局。
- 地方按省→府州→县逐级进入，全国与省内地名搜索保留完整任所路径。贵州四个羁縻直隶州复用原分类及州衙入口，州领县保留上级属州。
- 天下首页先显示省域矢量概览；约 3.2MB 的地形图仅在打开地图时载入。地图可用缩放按钮放大、滑动查看，并通过省份选择进入地方。
- 京师改为官署卡片；部内分司默认折叠并在展开后挂载。朝班、州县衙署采用短场景图和姓名/官职卡片，不在手机挂载桌面宽场景。沿用已有年度、任所和官职匹配，未知人名不写成空缺。
- 人物页可在当前年与全部人物间切换，搜索及首批20项分页复用现有目录，不增加逐字网络请求。手机搜索进入人物后可返回原关键词；全站官职搜索不会继承背后州县页的任所限制。
- 人物、官职、官署、搜索和年份面板按全屏阅读排版，固定标题和关闭入口，正文独立滚动；履历按年代纵向排列。输入字号至少16px，主要操作区至少44px，处理安全区与软键盘可见视口。
- 验证：TypeScript、修改文件的 oxlint、9项组件服务端渲染/真实数据检查及最终干净构建通过。`node scripts/check-mobile.mjs` 的结果保存于 `work/mobile-checks.json`。本轮没有进行手机实机或浏览器视觉测试，不将静态检查称为设备验收。
- 最终资源为 `assets/index-CRXuif6z.js` 和 `assets/index-sVmyPYDq.css`，gzip约159KB / 58KB。历史数据库与读取缓存版本保持 `93313cbf2a6b549e`，未删除人物、史料或访问量数据。

## 访问分析后台与用户反馈（2026-09-07）

- 发布版本 `20260906T200757Z`，当前前台资源 `assets/index-7pPLRSQY.js`、`assets/index-ByXu2364.css`。后台入口 `https://dmwc.cc/admin/`，采用独立 PHP 页面和样式，不给前台加载后台页面代码。
- 后台支持时间范围、来源 IP、省份、设备和浏览器筛选，以及每日访问趋势、分布和逐次访问明细。手机、电脑、平板按浏览器标识推断；IP 只读取服务器连接地址，不采信客户端伪造的转发头。入口丢弃查询参数，来源只保留域名。
- 现有累计访问量继续保留。每个访问请求的明细、计数令牌和日汇总在同一事务内保存，重试只计一次；历史 IP 明细不补造。新增 `atlas_ops_meta`、`atlas_visit_events`、`atlas_feedback`、`atlas_ops_limits`、`atlas_admin_users` 五张表，未改动历史人物及任官数据。
- IP 归属使用官方 ip2region v3.17.0 离线 IPv4/IPv6 库，来源及 SHA-256 记录在 `work/ops-geo/manifest.json`，许可随库部署。程序与数据库位于 `/others/dmwc-admin/geo/`，不公开、不逐次请求外部定位服务。正式主机单次 IPv4+IPv6 样本合计 5.91ms，不代表全部请求耗时。
- 私有设置及会话目录分别为 `/others/dmwc-admin/config.php`、`/others/dmwc-admin/sessions/`；复用已有 `/others/dmwc-history/config.php` 的数据库设置。`scripts/configure-ops.py` 按隐藏提示读取 DB/FTP 密码，先建新表、账号和私有配置，再发布新接口；重跑不重置已有管理员与私有密钥。
- 管理员初始账号为 `admin`，独立随机初始密码仅保存在本机忽略版本控制的 `work/后台登录信息.txt`，首次登录强制修改。登录包含会话失效、CSRF、来源检查和次数限制；密码变更使其他会话失效。所有管理页禁止缓存、公开索引与跨站嵌入。
- 前台电脑页头、手机页头及页脚提供反馈入口；填写手机号、类型和内容，自动附带当前页面与年份。手机使用全屏表单；连接未就绪时不开放填写，失败保留内容，重试去重。后台可查询反馈、查看手机号、更新处理状态及内部备注；不自动发送短信。隐私说明位于页脚。
- 验证：43项隔离 MySQL/本地 HTTP 检查、9项手机组件检查、TypeScript、oxlint、PHP语法、干净构建及差异检查通过。正式主机8项配置/数据表/私有目录/离线IP/隔离计数检查通过，一次性校验入口已删除。测试均未向正式访问量或反馈记录中加入测试数据。
- FTP 9个变更文件回读哈希一致；575个公开文件、首页及历史API通过独立公网校验。回执见 `work/ops-checks.json`、`work/ops-host-checks.json`、`work/ftp-deployment-receipt.json`。
- **尚待主机面板启用有效 SSL**：本轮公网证书仍为 self-signed certificate，已向用户说明并询问由其启用或提供管理面板入口。HTTP 后台与反馈均返回426并指向HTTPS。代码已部署、访问明细可记录，但后台正式登录和手机号反馈须在有效证书启用后使用；本轮未绕过证书校验发送管理密码，也未进行浏览器/手机实机验收。

## 手机弹框偏出屏幕修复（2026-09-07）

- 用户 iPhone 截图显示年号弹框向左上偏移半个弹框，标题和关闭按钮被裁掉。旧正式 CSS 同时保留 Tailwind 的独立 `translate:-50% -50%`，并将手机源码的 `translate:none; transform:none` 压缩成仅 `transform:translate(0,0)`，导致两种不同属性叠加；原有源码检查没有发现这个发布产物问题。
- 公共 Dialog 移除独立 translate 类，改为共享 `dialog.css`：桌面只使用一个 `transform` 居中，手机对同一个属性归零；过渡只改变透明度。手机尺寸、可见视口与软键盘区域由公共规则统一控制，反馈/隐私也使用同一套定位。
- 覆盖年号、地图、搜索、官署、官职详情、人物履历、年度名册、司礼监、帝位、反馈及隐私弹框。正文独立滚动与固定关闭入口保留。史料侧边抽屉本来没有半屏居中，不改其进出动效。
- 最终资源：`assets/index-CnFQFCvB.js`、`assets/index-DmapFgeb.css`；发布版本 `20260907T043401Z`。TypeScript、oxlint、9项原手机组件检查、6项最终压缩CSS回归检查通过；检查脚本 `scripts/check-dialog-css.mjs` 会直接读取当前构建的 CSS，防止相同压缩问题再次出现。本轮没有浏览器或 iPhone 实机复测，不将静态检查称为实机验收。
- 首次上传连接中断后，确认线上仍为上一版本再重试。最终3个变更文件 FTP 回读一致；575个公开文件、首页及原有API全部通过独立公网核验，带 `?v=20260907-dialog` 的新入口亦与本地最终页面哈希一致。
